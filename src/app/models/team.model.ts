export class Team {
    constructor(
        public name: string,
        public img: string,
        public sport: string,
        public season: string,
        public record: string,
        public description: string
    ) { }
}